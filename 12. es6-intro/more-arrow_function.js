const getTax = (ammount, taxRate) => (ammount * taxRate) / 100;
const add = (p, q) => p + q;

const getSquare = (x) => x * x;
console.log(getSquare(8));

const getHalf = (a) => a / 2;
console.log(getHalf(2));

// get first element
const getFirstElm = (nums) => nums[0];
console.log(getFirstElm([82, 15, 67, 42]));

// annonymous function
document.getElementById("").addEventListener("click", () => {
  // code
});

// annonymous function with parameter
document.getElementById("").addEventListener("click", (e) => {
  // code
});

/*

() => {
  // code
}

*/
