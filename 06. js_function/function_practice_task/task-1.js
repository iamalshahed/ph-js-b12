/**
 * Task-1
Take four parameters. Multiply the four numbers and then return the result
 */

function multiply(num1, num2, num3, num4) {
  const gunfol = num1 * num2 * num3 * num4;
  return gunfol;
}
const input = multiply(5, 2, 90, 45);
console.log(input);